<sidebar>
   <!--サイドバー-->
  <div class="container px-5 px-sm-0">
  <div class="row">
  <div class="col-md-3">
    <h2>User info</h2>
    <p><%= image_tag 'sample-author1.jpg' %></p>
    <table class="table">
      <tbody>
        <tr></tr>
        <tr>
          <th>name</th>
          <th><a href="<%= edit_user_registration_path(current_user) %>" class="user-name">
            <%= current_user.name %>
          </a>
        </tr>
        <tr>
          <th>introduction</th>
          <th></th>
        </tr>
      </tbody>
    </table>
    <div class="row">
      <a class="btn btn-outline-secondary btn-sm btn-block mb-3 " aria-current="page" href="<%= edit_user_registration_path(current_user) %>"><i class="fa-solid fa-user-gear"></i></a>
    </div>
<h2>New Book</h2>
<%= form_with model: @book do |f| %>
  <div class="form-group">
   
    <label for="book_title">Title</label>
    <input class="form-control book_title" type="text" name="book[title]" id="book_title">
    
  </div>
  <div class="form-group">
    <label for="book_opinion">Opinion</label>
    <input class="form-control book_body" name="book[body]" id="book_body">
  </div>
    <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book">
<% end %>
</div>
</div>
</div>
</sidebar>