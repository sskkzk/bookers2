      <div class="col-md-3">
      <h2>User info</h2>
      <p><%= image_tag @user.get_image(100,100) %></p>
      <table class="table">
        <tbody>
          <tr></tr>
          <tr>
            <th>name</th>
            <th><%= @user.name %></tr>
          <tr>
            <th>introduction</th>
            <th><%= @user.introduction %></th>
          </tr>
        </tbody>
      </table>
      
    <% if @user == current_user %>
    <div class="row">
      <%= link_to "<i class='fa-solid fa-user-gear'></i>".html_safe, edit_user_path(@user), class: "btn btn-outline-secondary btn-sm btn-block mb-3", aria_current: "page" %>
    </div>
    
<h2>New Book</h2>
<%= form_with model: @book do |f| %>
  <div class="form">
   <h6>Title</h6>
   <%= f.text_field :title, class:"form-control" %>
  </div>
  <p></p>
  <div class="form">
   <h6>Opinion</h6>
   <%= f.text_area :body, class:"form-control" %>
   </div>
   <p></p>
    <div class="form">
      <%= f.submit 'Create Book', class: "btn btn-success" %>
    </div>
<% end %>

 <% end %>
</div>
