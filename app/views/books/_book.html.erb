
      <div class="col-md-3">
      <h2>User info</h2>
      <p><%= image_tag 'sample-author1.jpg' %></p>
      <table class="table">
        <tbody>
          <tr></tr>
          <tr>
            <th>name</th>
            <th><a href="<%= edit_user_registration_path(current_user) %>" class="user-name">
              <%= current_user.name %>
            </a>
          </tr>
          <tr>
            <th>introduction</th>
            <th></th>
          </tr>
        </tbody>
      </table>
    <div class="row">
      <a class="btn btn-outline-secondary btn-sm btn-block mb-3 " aria-current="page" href="<%= edit_user_registration_path(current_user) %>"><i class="fa-solid fa-user-gear"></i></a>
    </div>
<h2>New Book</h2>
<%= form_with model: @book do |f| %>
  <div class="form">
   <h6>Title</h6>
   <%= f.text_field :title, class:"form-control" %>
  </div>
  <p></p>
  <div class="form">
   <h6>Opinion</h6>
   <%= f.text_area :body, class:"form-control" %>
   </div>
   <p></p>
  <div class="form">
   <%= f.submit 'Create Book', style: 'background-color: #32cd32; color: #fff; border: none; padding: 10px 20px; border-radius: 5px;' %>
  </div>
<% end %>
</div>
