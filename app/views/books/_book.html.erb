
      <div class="col-md-3">
      <h2>User info</h2>
      <p><%= image_tag @user.get_image(100,100) %></p>
      <table class="table">
        <tbody>
          <tr></tr>
          <tr>
            <th>name</th>
            <th><%= current_user.name %></tr>
          <tr>
            <th>introduction</th>
            <th></th>
          </tr>
        </tbody>
      </table>
    <div class="row">
      <%= link_to "<i class='fa-solid fa-user-gear'></i>".html_safe, edit_user_registration_path(current_user), class: "btn btn-outline-secondary btn-sm btn-block mb-3", aria_current: "page" %>
    </div>
<h2>New Book</h2>
<%= form_with model: @book do |f| %>
  <div class="form">
   <h6>Title</h6>
   <%= f.text_field :title, class:"form-control" %>
  </div>
  <p></p>
  <div class="form">
   <h6>Opinion</h6>
   <%= f.text_area :body, class:"form-control" %>
   </div>
   <p></p>
  <div class="form">
   <%= f.submit 'Create Book', style: 'background-color: #32cd32; color: #fff; border: none; padding: 10px 20px; border-radius: 5px;' %>
  </div>
<% end %>
</div>
